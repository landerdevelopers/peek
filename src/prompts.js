export const OCR_PROMPT =
  "Extract all visible text from this image, verbatim, exactly as it appears. " +
  "Preserve line breaks. Output ONLY the extracted text — no commentary, no markdown formatting, no code fences.";
